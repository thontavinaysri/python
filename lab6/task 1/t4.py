#25341a05l1 vinay
# match() checks only the beginning of the string, while fullmatch() checks the entire string.
# Therefore, match() can succeed when the string starts with digits, but fullmatch() fails if other characters appear later.